{
  "description": "Quality gate stop hook - runs type check, lint, and format checks before allowing session to complete",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/stop-quality-gate.sh",
            "statusMessage": "Running quality gate checks..."
          }
        ]
      }
    ]
  }
}
